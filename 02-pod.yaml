apiVersion: v1
kind: Pod
metadata:
  name: nginx #pod name
  labels:
    project: roboshop
    environment: dev
    purpose: pod
spec:
  containers:
  - name: nginx #name of conatiner
    image: nginx


