apiVersion: v1
kind: pod
metadata:
  name: nginx #pod name
  labels:
    project: roboshop
    environment: dev
    purpose: pod
spec:
  container:
  - name: nginx #name of conatiner
    image: nginx


